<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commander Interface</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f9; }
    </style>
    <% 
        // FIX: Define EJS variables using 'var' to ensure template-wide scoping and prevent 'Cannot access before initialization' errors
        var baseUrl = typeof baseUrl !== 'undefined' ? baseUrl : '/admin';
        var activePage = typeof activePage !== 'undefined' ? activePage : 'commander';
    %>
</head>
<body class="p-8">

    <div class="max-w-4xl mx-auto bg-white shadow-2xl rounded-xl p-8 space-y-8">
        <h1 class="text-4xl font-extrabold text-gray-800 border-b-4 border-red-500 pb-4">
            Remote Commander (Advanced)
        </h1>
        
        <!-- Navigation to Admin Pages -->
        <nav class="flex justify-center space-x-2 mb-8 border-b pb-4 text-sm">
            <a href="<%= baseUrl %>" class="px-3 py-1 rounded transition duration-200 text-<%= activePage === 'control' ? 'white bg-indigo-600 font-bold shadow-md' : 'gray-600 hover:bg-gray-100' %> font-medium">Control Panel</a>
            <a href="<%= baseUrl %>/config" class="px-3 py-1 rounded transition duration-200 text-<%= activePage === 'config' ? 'white bg-indigo-600 font-bold shadow-md' : 'gray-600 hover:bg-gray-100' %> font-medium">Feed Config</a>
            <a href="<%= baseUrl %>/status" class="px-3 py-1 rounded transition duration-200 text-<%= activePage === 'status' ? 'white bg-indigo-600 font-bold shadow-md' : 'gray-600 hover:bg-gray-100' %> font-medium">System Status</a>
            <a href="<%= baseUrl %>/commander" class="px-3 py-1 rounded transition duration-200 text-<%= activePage === 'commander' ? 'white bg-indigo-600 font-bold shadow-md' : 'gray-600 hover:bg-gray-100' %> font-medium">Commander</a>
        </nav>

        <div class="p-6 bg-red-50 rounded-lg border border-red-300 shadow-md">
            <h2 class="text-2xl font-semibold mb-4 text-red-700">Send Kiosk Command</h2>
            <p class="text-gray-600 mb-4">Send raw commands (e.g., reboot, restart service, clear cache).</p>
            
            <div class="mb-4">
                <label for="command-input" class="block text-sm font-medium text-gray-700 mb-2">Command</label>
                <input 
                    type="text" 
                    id="command-input" 
                    placeholder="e.g., REBOOT_KIOSK"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-red-500 focus:border-red-500 shadow-sm"
                >
            </div>

            <button
                id="send-command-button"
                class="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-xl shadow-lg transition duration-150 ease-in-out transform hover:scale-[1.01] active:scale-[0.99] w-full text-lg">
                Execute Command
            </button>
        </div>

        <div class="p-6 bg-white rounded-lg shadow-md border border-gray-100">
            <h2 class="text-2xl font-semibold mb-4 text-gray-700">Command Log</h2>
            <p class="text-gray-500">No commands executed recently.</p>
        </div>
    </div>
</body>
<script>
    async function fetchCommands() {
    // Calls GET /api/commander/commands
    const response = await fetch('/api/commander/commands');
    // ... logic to populate <select id="command-select">
    }
async function executeCommand() {
    const command = commandSelect.value;
    const secretKey = secretKeyInput.value.trim();
    
    // Calls POST /api/commander/command/execute
    const response = await fetch('/api/commander/command/execute', {
        method: 'POST',
        headers: { 
            'Content-Type': 'application/json',
            // CRITICAL: This header is checked by the 'authenticate' middleware
            'X-Secret-Key': secretKey 
        },
        body: JSON.stringify({ command: command })
    }); 
}   
</script>
</html>
